/* SPDX-License-Identifier: MPL-2.0 */
/* Build policy for the complete FreeBSD PCI bridge core. */

#ifndef EDGEOS_COMPAT_FREEBSD_OPT_PCI_H
#define EDGEOS_COMPAT_FREEBSD_OPT_PCI_H

/*
 * PCI hot-plug support remains disabled until EdgeOS exposes the matching
 * native slot-event service.  The standard bridge and nested-bus paths are
 * compiled in full.
 */

#endif
